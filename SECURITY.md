# Security and privacy

The components should not send book content, cover images, ISBN values, or user identifiers to an external service unless the consuming application explicitly does so.

Do not commit customer books, private cover art, credentials, or production data. Validate image URLs and avoid unsafe HTML injection when integrating book metadata.
